# Combine DSW Plan
A simple web application using FastAPI that allows users to select a date and a plan from this semester. Uses simple HTML forms and CSS for styling. The backend processes the form data and scrapes the DSW plan for the selected date and plan, returning it to the user in a combined form.

Might or might not be hosted at: https://combine-dsw-plan.onrender.com/